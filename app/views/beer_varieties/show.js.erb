$('#beer_variety').html("<%= escape_javascript(render :partial => 'varieties') %>");
var checkedId = $('form.rating_ballot > input:checked').attr('id');
$('form.rating_ballot > label[for=' + checkedId + ']').prevAll().andSelf().addClass('bright');