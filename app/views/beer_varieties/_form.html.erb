<%= form_for([@beer, @beer_variety]) do |f| %>
  <% if @beer_variety.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_variety.errors.count, "error") %> prohibited this beer_variety from being saved:</h2>

      <ul>
      <% @beer_variety.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :alcohol_percentage, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :alcohol_percentage, {:type => 'number'} %>
      </div>
    </div>


    <div class="control-group">
      <%= f.label :kind_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.select :kind_id, @kinds.map { |kind| [kind.name, kind.id] } %>
      </div>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
